METHOD 1:

class Solution {
public:
    int maxDepth(TreeNode* root) {
        if(root==NULL) return 0;
        int left=maxDepth(root->left);
        int right=maxDepth(root->right);
        int ans=max(left, right);
        return ans+1;
    }
};

T.C. = O(N) N is the number of nodes.
S.C. = O(H) H is the height of tree.
