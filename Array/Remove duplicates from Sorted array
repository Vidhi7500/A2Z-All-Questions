METHOD 1:

class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        int n=nums.size();
        int num=nums[0];
        int j=1;
        for(int i=1;i<n;i++){
            if(num!=nums[i]){
                nums[j]=nums[i];
                num=nums[i];
                j++;
            }
        }
        return j;
    }
};

T.C. = O(n)
S.C. = O(1)
